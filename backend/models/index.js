const User = require('./User');
const StudentProfile = require('./StudentProfile');
const AlumniProfile = require('./AlumniProfile');
const MentorshipRequest = require('./MentorshipRequest');
const ReferralRequest = require('./ReferralRequest');
const { ChatMessage, Conversation } = require('./ChatMessage');

module.exports = {
    User,
    StudentProfile,
    AlumniProfile,
    MentorshipRequest,
    ReferralRequest,
    ChatMessage,
    Conversation
};
